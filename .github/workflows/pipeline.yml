name: DevOps CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.putraez }}
          password: ${{ secrets.dckr_pat_-LWKz18djT89zL8pKS2ZcvriMps }}

      - name: Build Image
        run: docker build -t putraez/myapp:v1 .

      - name: Push Image
        run: docker push putraez/myapp:v1